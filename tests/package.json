{
  "name": "@broker/tests",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "test": "node --test --test-reporter spec dist/policy/*.test.js",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@broker/common": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "@types/node": "^20.14.0"
  }
}
